[package]
name = "termhostd-android-jni"
version = "0.1.0"
edition = "2021"

[lib]
name = "termhostd"
crate-type = ["cdylib"]

[dependencies]
jni = { version = "0.21", default-features = false, features = ["invocation"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }


